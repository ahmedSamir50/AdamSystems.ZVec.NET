diff --git a/thirdparty/FastPFOR/CMakeLists.txt b/thirdparty/FastPFOR/CMakeLists.txt
index 6ff7b80..aaafd21 100644
--- a/thirdparty/FastPFOR/CMakeLists.txt
+++ b/thirdparty/FastPFOR/CMakeLists.txt
@@ -11,6 +11,11 @@ elseif(CMAKE_OSX_ARCHITECTURES MATCHES "arm64")
     set(_FASTPFOR_NEEDS_SIMDE TRUE)
 elseif(CMAKE_SYSTEM_NAME STREQUAL "iOS")
     set(_FASTPFOR_NEEDS_SIMDE TRUE)
+elseif(MSVC AND (
+        CMAKE_CXX_COMPILER_ARCHITECTURE_ID MATCHES "ARM64"
+        OR "$ENV{VSCMD_ARG_TGT_ARCH}" STREQUAL "arm64"))
+    # MSVC amd64->arm64 cross: also key off compiler / VS target arch.
+    set(_FASTPFOR_NEEDS_SIMDE TRUE)
 endif()
 
 # RISC-V: patch FastPFOR common.h to include SIMDe header for __riscv
